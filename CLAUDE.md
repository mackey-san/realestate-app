# CLAUDE.md

このファイルは、このリポジトリで Claude Code (claude.ai/code) が作業する際のガイダンスを提供します。

## プロジェクト概要

realestate-app は不動産関連のアプリケーションです。
（プロジェクトの詳細な構成・技術スタックは、実装が進み次第このセクションに追記してください。）

## Git 運用ルール

- **コードを変更するたびに、必ず GitHub にプッシュすること。** 作業を区切りのよい単位（1つの修正・1つの機能追加など）でコミットし、コミットしたら都度リモートリポジトリ（`origin`）にプッシュする。ローカルにのみ変更を残したままにしない。
- コミットメッセージは変更内容が分かるように簡潔に記述する（何を・なぜ変更したか）。
- 破壊的な操作（`git push --force`、`git reset --hard`、履歴の書き換えなど）は、明示的に指示された場合を除き行わない。
- 機密情報（APIキー、パスワード、`.env` ファイルなど）は絶対にコミット・プッシュしない。
- ブランチ運用や PR フローに追加のルールが決まったら、このセクションに追記する。

## 開発コマンド

（ビルド・テスト・Lint などのコマンドが決まり次第、このセクションに追記してください。）
